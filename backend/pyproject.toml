[project]
name = "shaddy-backend"
version = "0.1.0"
description = "Shaddy publishing API — stores published shader Recipes, serves the gallery, verifies Supabase JWTs."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.29",
  "pydantic-settings>=2.4",
  "python-jose[cryptography]>=3.3",
  "python-multipart>=0.0.9",
  "alembic>=1.13",
  "greenlet>=3.0",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "httpx>=0.27"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.setuptools]
packages = ["app"]
