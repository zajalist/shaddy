// Shared diagram primitives — colour aliases, stroke width, and fonts that
// every per-group diagram module consumes. Re-exported from the library's
// style layer so there is a single source of truth.

export { DIA, DIA_STROKE, DIA_FONT_MONO, DIA_FONT_DISPLAY } from '../style';
